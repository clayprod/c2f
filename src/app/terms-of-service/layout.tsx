// This layout is intentionally left blank to avoid conflicts with the page structure
// The public legal pages include all necessary layout components directly in the page file
export default function PublicLegalLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}